#include <iostream>
#include <vector>
int main()
{
    int size;
    std::cin>>size;
    std::vector<int> array(size);
    for(int i=0;i<size;i++)
    {
        std::cin>>array[i];
    }
    for(int i=0;i<n;i++)
    {
        
    }
}